import { FC } from 'react'
import './style.scss'

const SideBar: FC = () => {
  return (
    <main className="auth-sidebar">
      <section className="auth-drawing-area" />
    </main>
  )
}

export default SideBar
